#pragma once
#include <raylib.h>

inline constexpr Vector2 WINDOW_SIZE = {800.f, 600.f};
inline constexpr Vector2 WINDOW_CENTER = {WINDOW_SIZE.x / 2.f, WINDOW_SIZE.y / 2.f};

inline constexpr const char *WINDOW_TITLE = "Traffic Lights Simulator";
