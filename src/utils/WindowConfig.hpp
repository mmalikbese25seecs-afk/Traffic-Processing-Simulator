#pragma once
#include <raylib.h>

namespace CFG
{
    inline constexpr Vector2 WINDOW_SIZE = {800.0f, 600.0f};
    inline constexpr Vector2 WINDOW_CENTER = {WINDOW_SIZE.x / 2.0f, WINDOW_SIZE.y / 2.0f};

    inline constexpr const char *WINDOW_TITLE = "Traffic Lights Simulator";
}
